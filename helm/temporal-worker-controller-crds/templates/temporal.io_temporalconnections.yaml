---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: temporalconnections.temporal.io
spec:
  group: temporal.io
  names:
    kind: TemporalConnection
    listKind: TemporalConnectionList
    plural: temporalconnections
    shortNames:
    - tconn
    singular: temporalconnection
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Age
      jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          TemporalConnection is deprecated. Use Connection instead.
          New creates are rejected by the validation rule below (requires k8s >= 1.27).
        properties:
          apiVersion:
            type: string
          kind:
            type: string
          metadata:
            type: object
          spec:
            description: Spec is preserved as-is for backward compatibility.
            x-kubernetes-preserve-unknown-fields: true
          status:
            description: Status reflects the migration state.
            properties:
              conditions:
                items:
                  description: Condition from the Kubernetes meta/v1 API.
                  properties:
                    lastTransitionTime:
                      format: date-time
                      type: string
                    message:
                      type: string
                    observedGeneration:
                      format: int64
                      type: integer
                    reason:
                      type: string
                    status:
                      type: string
                    type:
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        type: object
        x-kubernetes-validations:
        - message: TemporalConnection is deprecated and no longer supported. Create
            a Connection instead.
          rule: oldSelf != null
    served: true
    storage: true
    subresources:
      status: {}
